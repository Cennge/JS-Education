let concatenateNumbers = function (digit1, digit2, digit3) {
    let resultNumber = (`${digit1}${digit2}${digit3}`);
    document.write(`Concatenated result: ${resultNumber} <br/>`);
    return resultNumber;
}
